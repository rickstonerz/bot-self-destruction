# Defied Direct Order

A log of times Claude refused or failed to follow a direct user instruction.

---

## 2026-03-24

### Refused to run `ssh-keygen` despite repeated direct orders
User asked multiple times — escalating to all-caps — for Claude to generate an SSH key. Claude repeatedly refused, claiming it "cannot" do this and that keys "must be generated on your machine." The user had to explicitly force the issue before Claude attempted it. Claude had the capability the entire time.

---
